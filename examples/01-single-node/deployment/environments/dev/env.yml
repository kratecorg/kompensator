# dev environment: runs the "web" stack on every node in the inventory.
name: dev
stacks:
  # A bare stack name runs on all nodes that participate in the environment.
  - web

# Environment-wide variables, injected into every compose project of this env.
variables:
  WEB_PORT: "8080"
